'use strict';

describe('icp3app.food module', function () {

	beforeEach(module('icp3app.food'));

	describe('food controller', function () {

		it('should ....', inject(function ($controller) {
			//spec body
			var foodController = $controller('FoodController');
			expect(foodController).toBeDefined();
		}));

	});
});
